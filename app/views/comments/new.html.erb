<h1>Add Comment</h1>

<%= form_with(model: @comment, url: post_comments_path) do |f| %>
  <div class="comment">
    <%= f.label :comment %>
    <%= f.text_area :text, placeholder: 'enter comment...', required: true %>
  </div>
<div>
  <%= f.submit 'Create Comment' %>
</div>
<% end %>